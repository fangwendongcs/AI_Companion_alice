// TODO(legacy-scaffold): This controller is not mounted by the current local
// Node server. Keep it as an unused scaffold marker; new backend logic belongs
// under backend/routes and backend/services.
export function getUser(req, res) {
  res.json({ ok: true });
}
