// TODO(legacy-scaffold): This Express-style scaffold is not mounted by
// backend/server.js. Keep it only as a placeholder until the backend shape is
// finalized; do not add new API logic here.
export function registerApiRoutes(app) {
  return app;
}
